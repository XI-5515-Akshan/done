version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    container_name: calc-backend
    restart: always

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    container_name: calc-frontend
    depends_on:
      - backend
    restart: always